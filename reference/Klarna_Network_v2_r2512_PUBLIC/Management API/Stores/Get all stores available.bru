meta {
  name: Get all stores available
  type: http
  seq: 4
}

get {
  url: {{base_url}}/{{version}}/accounts/:partner_account_id/stores
  body: none
  auth: inherit
}

params:query {
  ~size: 
  ~starting_after: 
  ~ending_before: 
}

params:path {
  partner_account_id: {{partner_account_id}}
}

script:post-response {
  bru.setVar('store_id', res.body?.stores[0].store_id || null);
}
