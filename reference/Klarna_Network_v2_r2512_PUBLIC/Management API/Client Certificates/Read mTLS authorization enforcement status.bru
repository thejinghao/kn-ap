meta {
  name: Read mTLS authorization enforcement status
  type: http
  seq: 7
}

get {
  url: {{base_url}}/{{version}}/account/integration/client-certificates/enforcement
  body: none
  auth: inherit
}

tests {
  test("should be able to disable a client identifier", () => {
    const data = res.getBody();
    expect(res.getStatus()).to.equal(204);
  });
  
}
