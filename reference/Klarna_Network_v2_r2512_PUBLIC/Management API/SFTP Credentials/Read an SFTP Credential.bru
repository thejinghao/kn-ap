meta {
  name: Read an SFTP Credential
  type: http
  seq: 3
}

get {
  url: {{base_url}}/{{version}}/account/integration/credentials/sftp/:credential_id
  body: none
  auth: inherit
}

params:path {
  credential_id: {{credential_id}}
}
