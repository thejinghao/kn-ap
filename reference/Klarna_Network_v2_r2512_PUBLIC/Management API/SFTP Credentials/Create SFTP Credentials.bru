meta {
  name: Create SFTP Credentials
  type: http
  seq: 1
}

post {
  url: {{base_url}}/{{version}}/account/integration/credentials/sftp
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "description": "{{environment}} SFTP Credentials"
  }
}

script:post-response {
  bru.setVar('credential_id', res.body?.credential_id || null);
}
