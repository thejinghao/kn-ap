meta {
  name: Read Payment Capture
  type: http
  seq: 6
}

get {
  url: {{base_url}}/{{version}}/accounts/:partner_account_id/payment/captures/:payment_capture_id
  body: none
  auth: inherit
}

params:path {
  partner_account_id: {{partner_account_id}}
  payment_capture_id: {{payment_capture_id}}
}
