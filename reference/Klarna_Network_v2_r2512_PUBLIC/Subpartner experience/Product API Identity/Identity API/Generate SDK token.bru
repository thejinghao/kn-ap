meta {
  name: Generate SDK token
  type: http
  seq: 3
}

post {
  url: {{base_url}}/{{version}}/identity/sdk-tokens
  body: none
  auth: inherit
}

headers {
  Klarna-Customer-Token: {{customer_token}}
}
